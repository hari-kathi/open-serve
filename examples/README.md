# Examples

- `kind-local/` *(planned)* — CPU-only demo on kind: KubeRay + chart with a tiny model, exercising gateway auth → routing → probe → status page without GPUs.
- `gcp-quickstart/` *(planned)* — fresh GCP project to serving endpoint using the reference Terraform and the Flux deployment layout.
